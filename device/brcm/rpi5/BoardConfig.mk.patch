diff --git a/BoardConfig.mk b/BoardConfig.mk
index a93cb46..d0066a6 100644
--- a/BoardConfig.mk
+++ b/BoardConfig.mk
@@ -58,6 +58,9 @@ TARGET_NO_RECOVERY := true
 BOARD_SEPOLICY_DIRS += device/brcm/rpi5/sepolicy
 BOARD_KERNEL_CMDLINE += androidboot.selinux=permissive
 
+# Display: uses the connected monitor's native EDID (e.g. 1920x1080 via DDC).
+# No force_hotplug / EDID-firmware override so 1080p panels drive at native res.
+
 # Treble
 TARGET_COPY_OUT_VENDOR := vendor
 
